# Shell scripts run inside Linux containers; a CRLF shebang (`#!/usr/bin/env bash\r`)
# breaks them. Pin them to LF so a Windows checkout (core.autocrlf=true) cannot corrupt
# the copy that gets built into the image.
*.sh text eol=lf
